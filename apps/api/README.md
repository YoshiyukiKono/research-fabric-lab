# API app

MVP role:

- Receive intent from the portal.
- Call the spec-generator agent.
- Validate generated YAML against JSON schemas.
- Create a GitHub branch, commit, and Pull Request.

Suggested first implementation: FastAPI.
