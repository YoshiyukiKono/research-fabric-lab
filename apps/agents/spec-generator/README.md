# Spec generator agent

This agent converts natural language research intent into:

- `experiment.yaml`
- `agenttask.yaml`
- `README.md`
- optional `job.yaml` placeholder

The agent should not apply Kubernetes resources directly.
