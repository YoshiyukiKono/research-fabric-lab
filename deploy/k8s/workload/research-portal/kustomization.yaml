apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - configmap-app.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - serviceaccount.yaml
  - role.yaml
  - rolebinding.yaml

# Do not include secret-example.yaml in resources.
# Create the real Secret manually:
#
# kubectl --context agent-lab -n research create secret generic openai-api-key \
#   --from-literal=OPENAI_API_KEY='sk-...'
